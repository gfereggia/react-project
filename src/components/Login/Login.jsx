import React from "react";

//components
import Form from '../Form/Form';

const Login = () => {
    const title = 'Login'
    return (
        <>
            <Form formTitle={title}/>
        </>
    );
};

export default Login;
